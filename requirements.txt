# Core web framework
fastapi
uvicorn[standard]

# Templating and file uploads
jinja2
python-multipart

# Async HTTP client (ComfyUI + Ollama communication)
httpx

# Image handling
Pillow

# Video trimming fallback (when ffmpeg not available)
opencv-python-headless
